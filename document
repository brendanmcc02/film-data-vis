get array of 100 latest films ("filmID", "myRating", "watchedInCinema")

for each of the 100 latest films:
	if film is already in db:
		update:
			metascore
			myRating
			imdbRating
			numberOfVotes
			watchedInCinema
	else:
		use api to get raw film
		filter raw film and append to db
